<%= form_for(@universidad) do |f| %>
  <% if @universidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@universidad.errors.count, "error") %> prohibited this universidad from being saved:</h2>

      <ul>
      <% @universidad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field small-3 columns">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>

  <div class="field small-3 columns">
    <%= f.label :abreviacion %><br />
    <%= f.text_field :abreviacion%>
  </div>
  <div class="field small-3 columns">
    <%= f.label :tipo %><br />
    <%= f.select :tipo, ["PUBLICA", "PRIVADA"] %>
  </div>

  <div class="field small-3 columns">
    <%= f.label :ciudad_id %><br />
    <%= f.collection_select :ciudad_id, Ciudad.all, :id, :nombre %>
  </div>

  <div class="actions small-3 coumns">
    <%= f.submit "Guardar" , :data => { :confirm => 1 }%>
  </div>
<% end %>
